---
- name: Ping presentation
  hosts:
    - '&tag_Environment_{{ vpc | normalize_ec2_tags }}'
    - '&tag_Role_presentation_app'

  tasks:
    - ping:

- name: Ping indexer
  hosts:
    - '&tag_Environment_{{ vpc | normalize_ec2_tags }}'
    - '&tag_Role_indexer_app'

  tasks:
    - ping:

- name: Ping mint
  hosts:
    - '&tag_Environment_{{ vpc | normalize_ec2_tags }}'
    - '&tag_Role_mint_app'

- name: Ping openregister
  hosts:
    - '&tag_Environment_{{ vpc | normalize_ec2_tags }}'
    - '&tag_Role_openregister_app'


  tasks:
    - ping:
