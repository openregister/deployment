---
- name: Install python depedencies
  yum: name="{{ item }}" state=installed
  become: true
  become_user: 'root'
  with_items:
    - 'python27-psycopg2'

- include: databases.yml
- include: user.yml
